const engWord = document.getElementById('eng'),
      rusWord = document.getElementById('erus'),
      inputs = document.getElementsByClassName('input'),
      addButton = document.getElementsById('add-word-btn'),
      table = document.getElementById('table');

let words;
let deleteBtn;

// функция конструктор для создании языковой пары 
const CreateWord = (english, russian) => {

}
